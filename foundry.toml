[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"                 # matches pragma; final compiler ADR (0.8.36 candidate) owed
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
bytecode_hash = "none"
via_ir = false
extra_output = ["storageLayout"]
build_info = true
ffi = false

[fuzz]
runs = 512

[invariant]
runs = 64
depth = 64
fail_on_revert = false
